Added spacing with following regexes
```
([\)\w\d\:\]]) ?([\=\*\+\-\/\\]) ?([\(\w\d\:\[])
```
and
```
([\)\w\d\:\]]) ?(,) ?([\(\w\d\:\[])
```
and
```
([\)\w\d\:\]]) ?(=) ?([\-\(\w\d\:\[])
```
and
```
([\)\w\d\:\]]) ?(\\) ?([\-\(\w\d\:\[])
```
